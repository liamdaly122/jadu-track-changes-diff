# Real Jadu HTML samples

Drop real Jadu Track Changes HTML files here as `.html` files.

These files are gitignored — they will not be committed to the repository.

Phase 6 will use these for integration tests.
